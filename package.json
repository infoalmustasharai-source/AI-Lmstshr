{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "dev:backend": "pnpm -C artifacts/api-server run dev",
    "dev:frontend": "pnpm -C artifacts/al-mustashar run dev",
    "dev:all": "pnpm run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:backend": "pnpm -C artifacts/api-server run build",
    "build:frontend": "pnpm -C artifacts/al-mustashar run build",
    "start:backend": "pnpm -C artifacts/api-server run start",
    "start:frontend": "pnpm -C artifacts/al-mustashar run serve",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "clean": "rm -rf node_modules **/node_modules **/dist **/.next && pnpm install"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
